<p id="notice"><%= notice %></p>

<h1>Ruta</h1>

<table>
  <thead>
    <tr>
      <th>Nombre ruta</th>
      <th>Estado</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ruta.each do |rutum| %>
      <tr>
        <td><%= rutum.nombre_ruta %></td>
        <td><%= rutum.estado %></td>
        <td><%= link_to 'Show', rutum %></td>
        <td><%= link_to 'Edit', edit_rutum_path(rutum) %></td>
        <td><%= link_to 'Destroy', rutum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rutum', new_rutum_path %>
